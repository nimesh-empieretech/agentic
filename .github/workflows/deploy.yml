name: Auto Deploy Agentic

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to EC2 with SSH
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            cd /home/ec2-user/agentic
            git pull origin main
            source venv/bin/activate
            pip install -r requirements.txt
            pm2 restart agentic
            pm2 save
            pm2 list
